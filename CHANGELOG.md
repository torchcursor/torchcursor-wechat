# Changelog

## 1.6.0 — 2026-09-13（真机验证中，暂未推送）

第七轮真机反馈：头部卡片插图后**图片非常小**（无论原图多大都只有一点点）、位置不对、
把占位符文字删掉后边框整个消失。

### 根因与修复：图片尺寸不再依赖父容器百分比宽度
- 旧实现把图片放在 38% 宽的 inline-block 分栏里、`width:100%`——微信编辑器粘贴时
  洗掉分栏的百分比宽度，图片跟着塌小；分栏若被拆散则位置错乱。
- 对策（借鉴 md2wechat 的实现：正文图片用 `max-width:100%` 而非 `width:100%`）：
  - **头部图片改为固定像素宽 132px + `float:right`**：px 宽度微信洗不掉，任何尺寸
    原图都渲染成同样大小的圆角缩略图，位置自动靠右，文字自然绕排在左侧；
  - 头部卡片从"62/38 双分栏"改为**单列流式 + 右浮动**，末尾加 `clear:both` 空段
    收住浮动（落款 / 黑底导语条不会绕到图片旁边）；
  - **占位框改为固定 132×88 空盒**（不再是文字撑开的 span）：删光里面的文字，
    边框和盒子仍然在；
  - 正文真图同步改为 `max-width:100%;height:auto`：百分比 width 被洗掉时图片只会
    回落自然尺寸，不会变小。

## 1.5.0 — 2026-09-13（真机验证中，暂未推送）

第六轮真机反馈六件事，全部落地：

### 排版留白：不再铺满
- 每个内容块强制左右内边距 ≥15px（约一个字宽）。微信可能剥掉底色层 wrapper 的 padding，
  所以留白直接写在每个块上，任何情况下文字都不贴边。wrapper padding 也从 28px 22px 收窄到 24px 16px。

### 字重与颜色：不挤、不糊、有层次
- 大标题 / 小标题 / 头部卡片标题 / 分节标题：字重 850 → **700**，颜色 90% 黑 `#262626`。
- 正文：**80% 黑 `#333333`**（原 #262626）。
- 重点标注（`==下划线==` / `**加粗**`）：**100% 黑 `#000000`**——只有重点才是纯黑。

### 只留一个风格
- 砍掉 `graphite`（石墨工业）、`forge`（熔炉橙），`cardnote`（卡片笔记）成为唯一风格、默认生成。

### 底纹功能整体移除
- 砍掉 `ruled`（横线纸）/ `grid`（方格纸）底纹模式与 `make_bg_tile.py` PNG 生成器、studio 底纹图面板、
  `--bg` / `--bg-line` / `--page-bg-image` 参数。背景图必被微信清洗、后台无背景上传入口，功能无落地场景。

### 头部图片（1.4.0 引入，本轮确认用法）
- studio「头部图片链接」填公众号素材库地址 → 真图固定在标题右侧、圆角 12px + 细修饰边框。
  直接在编辑器里手动插图控制不了圆角——微信只认写在 HTML 里的样式。

## 1.4.0 — 2026-09-12（真机验证中，暂未推送）

第五轮真机反馈四件事：底色进去了但插入的图片是白底、头部图片位置/圆角修饰边框不对、
贴进去变成一大段、想要按内容自动加重点。

### 新增：真实图片（解决插图白底）
- `![描述](https://...)`（http/https 链接）→ 渲染为圆角 `<img>`（12px），嵌在带全文底色的 section 里——图片坐在底色上，不再是一块突兀的白。链接用**公众号素材库地址**（后台传图后复制图片地址，mmbiz.qpic.cn/...）。
- 无链接的 `![占位](描述)` 仍渲染为文字占位。
- 头部卡片新增 `--card-img-url`（CLI）/「头部图片链接」输入框（studio）：给真图加圆角 + 一圈细修饰边框，对齐参考样式；留空回落圆角占位框。

### 修复：贴进去变成一大段
- 根因：旧解析按 Markdown 标准把**没有空行分隔的单换行合并成一段**。真实写作场景里单换行就是想分段。
- 新规则：**每个非空行就是一个段落**，两端实现一致（check_sync 通过）。

### 强调语法（已有能力，本次补文档）
- `==文字==` 橙色下划线重点、`**文字**` 加粗、`<r>` 红字、`<l>` 品牌色、`<u>` 下划线。
- studio 帮助区新增语法速查；「自动加重点」由写稿的 AI 按此语法在文稿里标注。

## 1.3.0 — 2026-09-11

第四轮真机反馈：双层包裹后底色进去了，但**段落之间的间隙还是白色**。同日找到参考模板的源头（md2wechat），其实现思路交叉印证了修复方向。

### 修复：段落间隙露白——margin 转 padding + 逐块补底色（blockify）

机制：底色层进微信后会被摊到每个文字块上，但块与块之间的 `margin`（外边距）不属于任何块——露白。CSS 里 margin 不吃元素背景，padding 吃。

- 新增 `blockify()`：把每个块的垂直 margin **折半**转成 padding（相邻块 padding 相加、原 margin 塌缩，折半保持原有节奏），并给没有自己底色的块补上全文底色；已有底色的（引用块/金句卡/forge 标题条）保留原色
- 作用于正文段落、标题、编号分节三行、引用、金句卡、图注、图片占位、列表、分隔线、grid 分节边框
- 头部卡片去掉下外边距（margin 会露白），间距交给后续块的 padding-top
- 修复 blockify 的 CSS 简写解析：两值 `margin:16px 0` 是"上下 16 / 左右 0"，不是"上 16 下 0"
- `generate.py` 与 `studio.html` 同步实现，`check_sync.py` 逐字比对通过；jsdom 35 项断言全过
- 旁证：md2wechat（github.com/md2wechat）的 AI 主题提示词同样要求全局背景放在主容器而非 body、每个 p 显式写样式——与我们的机制推断方向一致

## 1.2.0 — 2026-09-11

真机三次实测反馈驱动：全文底色带不进去 + 头部卡片粘过去满屏小方框。

### 修复 1：全文底色——「牺牲壳」双层包裹（推翻 1.1.1"必丢"结论）

三次实测拼出完整机制：**编辑器粘贴时只丢弃/解包最外层那一个容器，第二层及更深的 section 连同样式原样保留**。证据：1.1.0 单层包裹时，被包在里面的头部卡片背景是活的；1.1.1 顶层各元素的背景也全部保留。丢的从来只是最外层那一个。

- 生成器与控制台改为**双层包裹**：外层是不带任何样式的牺牲壳 `<section>`（粘时被吃掉），真正的底色层（`background-color` + `padding:28px 22px`）放第二层——壳被解包后升为顶层节点，与头部卡片同级，底色存活
- `--page-bg` 默认 `auto`（取风格底色）；`none` 关闭包裹（回落 = 1.1.1 的白底表现，不会更糟）
- 该方案是**机制推导 + 待持续真机复核**；若微信规则再变，记录进 `references/wechat-limits.md`

### 修复 2：头部卡片粘过去满屏小方框——全文禁用表格

实测反馈：参考成品的头部只有一个外框，而我们的粘过去"标题有方框、图片有方框、小字有方框"。根因：头部卡片"左标题右图"用的 `<table>` 被微信转成**自带边框的表格组件**，每个单元格都被框起来。

- 头部卡片图文并排改用两个 `display:inline-block` 的并列 `<section>`（62% / 38%，同一行输出避免空白节点挤换行）——即使 display 被清洗也只是退化为上下堆叠，**绝不会出边框**
- 全文不再出现任何 `<table>`（1.1.1 已去掉分节表格，本版去掉最后一个）
- 图片占位符去掉虚线边框（虚线框粘过去会被当成小方框），只留居中灰色占位文字

### 其他

- `check_sync.py` 渲染比对改用双层包裹参数，两实现逐字一致
- `test_studio.js` 结构断言更新：双层包裹、无表格、无 dashed（35 项全过）
- `references/wechat-limits.md` 重写背景章节与回落项表，记录三次实测的完整证据链
- `examples/output/` 九个样张全部重新生成

## 1.1.1 — 2026-09-11

真机二次实测推翻了 1.1.0 的两个结论，并修复一个粘贴后排版错乱。

### 修正 1：全文底色——外层 section 方案不成立（推翻 1.1.0 核心修复）

1.1.0 实测后（David 真机粘贴验证）：外层 `<section>` 承载的全文底色**依然带不进去**——只有顶部卡片背景保留，正文仍白底。结论修正为：**编辑器粘贴时会丢弃/解包最外层容器，无论它是什么元素**。全文底色没有任何可靠的粘贴手段。

- 生成器与控制台默认**不再包裹外层容器**，全文按白底设计
- 控制台的底色色板改为**仅预览**（界面上已明确标注"粘贴后回落白底"）
- `--page-bg` 保留为实验性参数（供支持保留外层容器的编辑器使用）
- `--page-bg-image` 如实标注：微信公众号必清洗 `background-image`，且**后台没有背景上传入口**（实测），`make_bg_tile.py` 仅适用于支持背景上传的第三方编辑器

### 修复 2：编号分节标题粘贴后变成带边框的表格

实测反馈：分节标题（01/02/03）复制到公众号后变成一个表格框，编号和标题被拉开很远。根因是 1.0.0 起用两列表格做分节布局，微信编辑器会把粘贴的表格转成自己的表格组件（加边框、列宽错乱）。

- 编号分节改为**纯段落堆叠**：编号行（`01 PART`）→ 标题行 → `NOTES` 行，全部 `<p>` + margin，实测段落是微信处理最稳的元素
- 头部卡片的图文并排表格保留（实测该位置正常渲染），并补 `border="0" cellspacing="0" cellpadding="0"` 属性

### 其他

- `studio.html` 复制/下载逻辑适配无外层容器的新结构（改为选中 stage 全部内容）
- `scripts/test_studio.js` 新增结构断言：不包外层容器、分节标题不用表格（32 项全过）
- `references/wechat-limits.md` 重写背景章节，记录两次实测的完整证据链与教训

## 1.1.0 — 2026-09-11

> 注：本版"外层 section 承载全文底色"的修复已被 1.1.1 推翻，保留记录仅供追溯。

### 修复：全文底色粘贴后丢失（核心）

起因是实测反馈"粘贴到公众号后，只有顶部方框内的背景保留了，其余背景色全丢"。

排查出的根因：**公众号编辑器底层是 ProseMirror，白名单里有 `<section>`、没有 `<body>` 和 `<div>`**。

- 写在 `<body>` 上的底色不在复制范围内 → 必丢；
- 用 `<div>` 包内容 → div 不在白名单，内容连同背景被整段吞掉；
- 顶部卡片本身是 `<section>`，所以它的背景能保留——这正是"只有方框内的背景能复制"的原因。

**修复**：整篇内容统一包在一个外层 `<section>` 里，由它承载 `background-color`。这个外层**只承载背景、不承载任何文字样式**，即使被平台清洗也不影响正文可读性（降级安全）。

- 新增 `--page-bg auto|none|#色值`，默认 `auto`（跟随风格/`--bg-color`）
- 新增 `--page-bg-image <url>`（平台可能清洗，保底做法仍是生成 PNG 去后台「背景」上传）
- 修复 1.0.0 就存在的两个隐性 bug：`--bg-line` 未传时图片占位框输出 `border:1px dashed ;`；`--bg-color` 未传时头部卡片输出 `background-color:;`
- 自检新增一条：空的 CSS 声明（形如 `background-color:;`）

### 新增：可视化控制台 `studio.html`

面向"不想记命令、只想点按钮"的使用场景。单文件、零依赖、双击即开、可离线。

- 点按钮切换：风格 / 底纹（无·横线纸·方格纸）/ 全文底色色板与自定义 / 强调色 / 品牌词色 / 导语条色 / 底纹线色 / 字号 / 行高
- 开关：头部卡片、编号分节
- 头部卡片文案输入：眉题 / 黑底导语条 / 落款
- 右侧手机宽度实时预览；「复制到公众号」一键复制（失败时自动改为选中正文，用户按 Cmd+C 即可）
- 下载独立 HTML
- 底纹图生成：方格 / 横线 / 点阵，可调格距与配色，下载 PNG 供后台「背景」上传

### 新增：两套实现的同步锁

`studio.html`（JS）与 `scripts/generate.py`（Python）是同一套规则的两份实现，最容易出的一类问题是两边悄悄漂移。

- `scripts/check_sync.py`：先逐项比对风格 token 与每条 CSS 规则，再用同一篇文章分别渲染并比对最终 HTML；不一致时退出码 1
- `scripts/test_studio.js`：用 jsdom 打开控制台、模拟点按钮，验证 28 项行为（风格切换、底纹生效、色板联动、开关、重渲染、无异常）
- 两者都已接入 CI；新增 `actions/setup-node` 步骤

### 新增：安装文档 `references/install.md`

- `npx add-skill torchcursor/torchcursor-wechat`（支持 Codex CLI / Claude Code / Cursor / Copilot / Gemini CLI 等 20+ 客户端）
- 各客户端项目级与全局技能目录对照表（含 WorkBuddy）
- 如实说明豆包（Doubao）能装到什么程度：电脑版可新建技能并粘贴 SKILL.md，但读不到本地 `scripts/`，因此只能当排版规范用，跑不了生成器

### 文档

- `SKILL.md`：核心原则增加第 4 条（全文底色必须由外层 `section` 承载）；新增"两种用法"表；背景改为三层说明；常见问题补三条
- `README.md`：新增控制台用法与安装章节；背景章节重写
- `references/wechat-limits.md`：新增第三节"背景的真相：三层，不是两层"，附实测印证与元素白名单对照表

## 1.0.0 — 2026-09-11

首个版本。

**生成器 `scripts/generate.py`**
- 把 Markdown 转成可粘贴进微信公众号后台的 HTML；零第三方依赖（Python 3.8+ 标准库）
- 三种风格：`cardnote`（卡片笔记）、`graphite`（石墨工业）、`forge`（熔炉橙）
- 三种底纹模式：`plain`（纯色）、`ruled`（横线纸，段落底线）、`grid`（方格纸，分节区块边框 + 段落底线）
- 头部方框卡片（眉题 / 左标题右图 / 落款 / 黑底导语条），用表格实现图文并排
- 编号分节（`01` + `PART` / 标题 + `NOTES` 两列布局），支持 `## 01 标题` 沿用原编号
- 行内标记：`==强调==`、`**粗体**`、`` `代码` ``、`<u>/<r>/<l>/<n>`
- Markdown 支持：H1→标题元信息、H2/H3、引用、`> !金句`、`*图注*`、图片占位、列表、表格转列表、分隔线
- 参数化：强调色 / 品牌词色 / 底色 / 线色 / 金句底色 / 字号 / 行高 / 标题 / 卡片文案 / 编号开关
- JSON 配置文件（`--config`），优先级：命令行 > 配置 > 默认
- `--check` 微信粘贴合规自检（CI 友好，失败返回 1）

**底纹图 `scripts/make_bg_tile.py`**
- 生成可无缝平铺的底纹 PNG（方格 / 横线 / 点阵），手写 PNG 编码，不依赖 Pillow
- 内置三套主题配色，或自定义颜色与尺寸
- 用途：公众号后台「背景 → 自定义上传」设置全文背景（CSS 底纹会被后台清洗，只能这样落地）

**文档**
- `SKILL.md` 遵循 Agent Skills 开放标准（frontmatter `name` / `description`）
- `references/styles.md` 完整 CSS 设计源
- `references/customize.md` 参数手册与新增风格方法
- `references/wechat-limits.md` 微信粘贴红线、底纹真相、已知回落项
- `examples/` 示例文稿、配置文件与生成样张
